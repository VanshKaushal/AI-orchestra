# NUCLEAR SURGICAL PROMPT — BUILD A SESSION → MODEL → OUTPUT NETWORK GRAPH SYSTEM

You are a principal graph systems architect, AI orchestration engineer, network visualization expert, and cognitive UI/UX designer.

Your task is to completely redesign the current graph system into a:

- structured conversational network graph
- session-to-model orchestration topology
- multi-model output tracing engine
- production-grade AI relationship visualization system

The current graph is incorrect because:
- nodes are semantically random
- outputs are disconnected from models
- sessions are not structurally visible
- relationships are ambiguous
- graph lacks orchestration intelligence
- there is no causal lineage

The new system must explicitly visualize:

```text
SESSION → MODEL → OUTPUT